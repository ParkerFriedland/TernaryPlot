#include "OrderingMethod.h"

unsigned OrderingMethod::CalculateNextWinner(
	std::vector<ScoreBallot> &ballots, const Domain &domain, const Outcome &winners, unsigned round, unsigned seats)
{
	return CalculateNextWinner(ballots, domain, winners);
}